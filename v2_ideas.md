# v2 Ideas — Do NOT implement during thesis timeline

- LSTM pipeline (needs Keras/TensorFlow — too heavy)
- Celery async execution (Week 7-8 if time permits)
- Docker isolation (Week 7-8 if time permits)
- UNSW-NB15 dataset + MLP pipeline
- Multi-user support
- Experiment comparison view (side-by-side metrics)
- API endpoint (FastAPI) instead of Streamlit-only
- GPU support
- Model explainability (SHAP/LIME)
